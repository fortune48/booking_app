<div id="appointment_index">
  <% @appointments.each do |a| %>
    <%= a.fixed_date %><br>
    <%= a.description %><br>
    <%= a.users.pluck(:first_name, :last_name).flatten %><br><br>
  <% end %>
</div>